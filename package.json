{
  "name": "typesnitch",
  "version": "0.0.7",
  "description": "A Simple Type Sniffer for JS",
  "main": "src/snitch.js",
  "scripts": {
    "test": "jest",
    "test:coverage": "jest --coverage",
    "test:silent": "npm test -- --silent --noStackTrace",
    "watch": "jest --watch --verbose",
    "watchall": "jest --watchAll",
    "build": "npm test",
    "lint": "npx eslint . --ext .js",
    "lint:fix": "npx eslint . --ext .js --fix"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/mfeyx/typesnitch.git"
  },
  "keywords": [
    "types",
    "type sniffer",
    "type detector",
    "type detection",
    "type checking",
    "type snitch",
    "check types",
    "type checker",
    "type check"
  ],
  "author": "Markus Feiks",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/mfeyx/typesnitch/issues"
  },
  "homepage": "https://github.com/mfeyx/typesnitch#readme",
  "devDependencies": {
    "eslint": "^7.3.1",
    "eslint-plugin-jest": "^23.17.1",
    "husky": "^4.2.5",
    "jest": "^26.1.0"
  },
  "husky": {
    "hooks": {
      "pre-commit": "npm run lint:fix",
      "pre-push": "npm test && npm run lint"
    }
  }
}
